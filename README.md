# _List of Questions_

**1. Arrays - 1 to 13**
- Find the largest element in an array
- Find the second-largest element without sorting the array
- Reverse an array without using another array
- Find the smallest and largest element in a single traversal
- Remove duplicate elements from an array
- Find the missing number from an array containing numbers from 1 to N
- Find all duplicates elements in an array
- Move all zeros to the end of an array while maintaining the order of other elements
- Find the intersection of two arrays
- Find the union of two arrays
- Find the maximum sum of a contiguous subarray
- Find two numbers whose sum equals a given target
- Find the third-largest element without sorting

**2. String**
- How do you reverse a String?
- Check whether a string is palindrome
- Find the first non-repeating character in a string
- Find duplicate characters in a string
- Count the frequency of each character in a string
- Check whether two strings are anagrams
- Find the longest word in a sentence
- Reverse the words in a sentence <br>
    Example: "I love JavaScript" > "JavaScript love I"
- Find the longest substring without repeating characters
- Check whether one string is a rotation of another string

**3. HashMap / HashSet**
- What us a HashMap/Map and when would you use it?
- What is the average time complexity of lookup in a HashMap?
- What is the difference between JavaScript Map and Object?
- When would you use a Set instead of an array
- Find duplicate numbers using a Set
- Find the frequency of elements in an array using a Map
- Find two numbers that add up to a target using a Map
- Find the first repeated character using a Map.
- Group an array of strings into anagrams. <br>Example: ["eat", "tea", "ate", "nat"]
- Given a list of users, efficiently find whether particular user ID exists

**4. Searching**
- What is linear search?
- What is its time complexity?
- What is binary search?
- What conditions must be satisfied before applying binary search?
- Implement binary search on a sorted array
- Find the first occurrence of a number in a sorted array
- Find the last occurrence of a number in a sorted array
- Find the position where a new element should be inserted into a sorted array
